# Refuse to install or run scripts unless the running Node/npm satisfy the
# exact versions pinned in package.json "engines". This enforces the pin
# regardless of which version manager (fnm, nvm, or none) is in use.
engine-strict=true
